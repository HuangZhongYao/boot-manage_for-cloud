# 开发文档

## 一、开发规范建议
- 类命名规范  
  好的命名规范见名知意
    + 实体类命名 使用Entity结尾如`UserEntity`、`OrderEntity`
    + DTO类命名 使用DTO结尾如 `AddUserDTO`、`EditUserDTO` 一看就明白是添加用户接口数据传输对象
    + VO类命名 使用VO结尾如 `UserVO`、`OrderVO`
    + 枚举类命名 使用Enum结尾如 `UserStatusEnum`、`OrderStatusEnum`
    + FeignClient类命名 使用Client结尾如 `UserClient`、`OrderClient`
    + Mapstruct 对象转换类命名 使用Convert结尾如 `UserConvert`、`OrderConvert`
    + 仓储层命名 使用Repository结尾如 `UserRepository`、`OrderRepository` 也可以用DAO结尾只要统一就好


- 继承基类  
  [org.githup.bm.common.base](bm-common/src/main/java/org/github/bm/common) 包下对常用类型如实体、DTO、VO、枚举、feignClient、Controller... 都进行了公共字段、方法进行了抽离方便复用。
    + 实体类继承 [AbstractBaseEntity.java](bm-common/src/main/java/org/github/bm/common/base/entity/AbstractBaseEntity.java), 如只需要继承 `id字段` 继承[AbstractIdEntity.java](bm-common/src/main/java/org/github/bm/common/base/entity/AbstractIdEntity.java)
    + DTO类继承 [BaseDTO.java](bm-common/src/main/java/org/github/bm/common/base/dto/BaseDTO.java)
    + 需要分页查询参数的类DTO继承  [BaseQueryPageInputDTO.java](bm-common/src/main/java/org/github/bm/common/base/dto/input/BaseQueryPageInputDTO.java)
    + VO类继承 [BaseVO.java](bm-common/src/main/java/org/github/bm/common/base/vo/BaseVO.java) 、[BaseIdVO.java](bm-common/src/main/java/org/github/bm/common/base/vo/BaseIdVO.java)、[BaseIdAndTimeAndOperationIdVO.java](bm-common/src/main/java/org/github/bm/common/base/vo/BaseIdAndTimeAndOperationIdVO.java)、[BaseIdAndTimeIdVO.java](bm-common/src/main/java/org/github/bm/common/base/vo/BaseIdAndTimeIdVO.java) 获得公共字段
    + 枚举类继承 [IEnumsValue.java](bm-common/src/main/java/org/github/bm/common/base/enums/IEnumsValue.java)、
    + Controller类继承 [BaseController.java](bm-common/src/main/java/org/github/bm/common/base/web/BaseController.java)
    + FeignClient 继承 [BaseFeign.java](bm-common/src/main/java/org/github/bm/common/base/fegin/BaseFeign.java)
    + Mapstruct的 对象转换 Convert 类继承 [AbstractConvert.java](bm-common/src/main/java/org/github/bm/common/base/convert/AbstractConvert.java) 继承 Entity、VO互转高频使用方法省去重复定义
    + Repository / Mapper / DAO 等类继承 [BaseMapperExtension.java](bm-common/src/main/java/org/github/bm/common/mybatis/BaseMapperExtension.java)

## 二、开启关闭服务调用验证
开启服务间调用验证适用于非本系统内服务调用拒绝请求  
在 Nacos `bm-resource.yml` 中配置
```yaml
bm:
  # 安全配置
  security:
    token:
      # token密钥
      secret: "zuuzYao-bm.9d4c8b1e3f5d7a0b2c9d8e7f6a5b"
      # token前缀
      prefix: "Bearer "
    # 内部调用认证配置
    internal-valid:
      # 开启后服务调用只能由服务之间发起调用外部不能调用适用于网关和服务都暴露在外网中情况
      enable: false
      # 认证令牌
      token: "bm-internal-token-zuuzYao-bm-Z2l0aHViLXp1dXVZYW8="
```
## 三、配置请求放行路径
在 Nacos `bm.yml` 中配置
```yaml
bm:
  # 安全配置
  security:
    # 跳过鉴权的uri路径
    skip-url:
     - /bm-example/**
     - /bm-websocket/**
     - /**/v3/api-docs
     - /**/v2/api-docs
     - /bm-auth/auth/login
     - /bm-auth/auth/refreshToken
     - /bm-datareport/ureport/**
     - /ureport/preview
     - /favicon.ico
     - /bm-auth/encryption/getEncryptionPublicKey
```

## 四、文件存储
存储方式支持local、alioss、minio 3 种存储方式  
在 Nacos `bm-resource.yml` 中配置
```yaml
server:
  port: 8210

storage:
  enabled: true
  # 对象存储名称 可选alioss、minio、local
  # 使用local存储时只部署一个服务实列,其它配置参数可忽略
  name: local
  # 对象存储服务的URL
  endpoint: http://127.0.0.1:9000
  # Access key就像用户ID，可以唯一标识你的账户
  access-key:
  # Secret key是你账户的密码
  secret-key:
  # 存储桶名称
  bucket-name: boot-manage-bucket
```

## 五、使用自定义数据源
因所有服务都导入了`bm.yml`共享配置， 默认使用了共享配置中的`mysql`、`redis`  
在 Nacos 中配置服务专用(`bm-xxx.yml`)配置文件覆盖共享配置  

列:  
如用户服务单独使用用户数据库则在`bm-user.yml`中配置数据源
```yaml 
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/bm-user?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 12356
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.zaxxer.hikari.HikariDataSource
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      password: 
      database: 12
```

## 六、启用分布式事务 Seata
推荐使用`Seata 2.0.0` 版本自带web控制台，可以直接使用本项目提供[配置文件（doc/seata/application.yml）](doc/seata/application.yml)启动。  
本项目使用默认AT模式，需要将seata和服务注册到同一个nacos集群同一个命名空间下。    
使用案例参考[ExampleGlobalTransactionsController.java](bm-service/bm-example/src/main/java/org/github/bm/example/web/ExampleGlobalTransactionsController.java)
1. 修改配置文件 `bm.yml` 只需修改 `seata.enabled`
    ```yaml
    # Seata 客户端配置
    seata:
      # 启用 Seata
      enabled: true
    ```
2. 配置seata服务端可参考 [doc/seata/application.yml](doc/seata/application.yml)
3. 确保事务参与者使用库都有`undo_log`表 [doc/seata/undo_log.sql](doc/seata/undo_log.sql)

## 七、启用流控组件 Sentinel
需要注意Sentinel 流控规则没有做持久化。重启后规则会丢失  
1. 修改配置文件 `bm.yml` 修改 `spring.cloud.sentinel.enabled`为启用
    ```yaml
    # 微服务公共配置      
    spring:
      cloud:
        sentinel:
          # 启用sentinel
          enabled: true
          # 取消Sentinel控制台懒加载
          eager: true
    ```
2. 配置Sentinel服务地址  
编辑 [LauncherConstant.java](bm-common/src/main/java/org/github/bm/common/launch/LauncherConstant.java)
    ```java
        package org.github.bm.common.launch;
        
        import org.github.bm.common.constant.AppConstant;
        
        public interface LauncherConstant {
        
            interface SentinelConstant {
                /**
                 * sentinel dev 地址
                 */
                String SENTINEL_DEV_ADDR = "127.0.0.1:8858";
        
                /**
                 * sentinel prod 地址
                 */
                String SENTINEL_PROD_ADDR = "192.168.1.219:8858";
        
                /**
                 * sentinel test 地址
                 */
                String SENTINEL_TEST_ADDR = "192.168.1.219:8858";
            }
        }
    ```
## 八、调整日志级别
找到nacos中配置文件 `bm.yml`, 修改`logging.level.root`
```yaml
#日志
logging:
  level:
    # 开发环境建议debug
    root: info
    com.baomidou.mybatisplus.samples.pagination: info
    org.springframework.web: info
    java.sql: info
    org.github.bm: debug
  # 日志配置文件
  config: classpath:logback-spring.xml
  # 日志文件位置, 默认值当前目录 boot-manage-logs
  path: boot-manage-logs
```